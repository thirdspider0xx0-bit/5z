module.exports = {
  // Export static HTML using next export
  output: 'export',

  // Useful for GitHub Pages project sites (replace '5z' with your repo name if different)
  basePath: '/5z',
  assetPrefix: '/5z/',

  // trailingSlash makes routing GitHub Pages simpler (index.html in each folder)
  trailingSlash: true,
}
